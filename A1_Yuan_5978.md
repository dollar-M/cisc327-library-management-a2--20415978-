## Name: **Ming Yuan**  
## Student ID: **20415978**
## Group Number: 5
| Function Name|Implementation status (complete/partial)|What is missing |
| :---------------- | :------: | :---- |
| add_book_to_catalog      |  complete  | None |
| borrow_book_by_patron     |  complete  | None |
| return_book_by_patron   |  incomplete | - Accepts patron ID and book ID as form parameters<br>- Verifies the book was borrowed by the patron<br>- Updates available copies and records return date<br>- Calculates and displays any late fees owed|
| calculate_late_fee_for_book |  incomplete   | - Calculates late fees for overdue books based on:<br>&nbsp;a.Books due 14 days after borrowing<br>&nbsp;b.$0.50/day for first 7 days overdue<br>&nbsp;c.$1.00/day for each additional day after 7 days<br>&nbsp;d.Maximum $15.00 per book<br>- Returns JSON response with fee amount and days overdue |
| search_books_in_catalog|  incomplete   | - Title search: Partial matching, case-insensitive<br>- Author search: Partial matching, case-insensitive<br>- ISBN search: Exact matching<br>- Return results in the same format as the main catalog<br> -q: search term<br>-type: search type (title, author, isbn)|
| get_patron_status_report     |  incomplete   | Display status report for a patron with the following:<br>&nbsp;- Currently borrowed books with due dates<br>&nbsp;- Total late fees owed<br>&nbsp;- Number of books currently borrowed<br>&nbsp;- Borrowing history |

The tests scripts are all stored in `tests` file. Here is a summery of them.

- `test_add_book.py`: Tests cases for adding books to the catalog(valid and invalid cases of isbn,title,copies). 

- `test_borrow_book.py`: Tests cases for borrowing books by patrons and book id(valid and invalid cases, including borrowing with invalid patron ID, borrowing non-exist book id, and borrowing a book that has no available copies left).

- `test_calculate.py`: Tests cases for calculating late fees for books. Checking format and type of returned dict, and tests with book that is not borrowed, with invalid patron ID or book id, or with valid patron ID and book id.

- `test_return_book.py`: Tests cases for returning books by patrons and book id(valid and invalid patron cases, including returning a book not borrowed, returning books with non-exist book id, and returning a book not borrowed by the patron).

- `test_search_books.py`: Tests cases for searching books by title, author (case insensitive), and ISBN (exact match)(valid and invalid cases). Also checking the format of the returned list of dict.

- `test_patron_status.py`: Tests cases for getting patron status report by patron ID(valid and invalid cases). Also checking the format and type of the returned dict and, and if two calls with the same patron ID return the same result.



